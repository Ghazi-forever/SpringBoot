
server:
  port: 8081

spring:
  application:
    name: products
  datasource:
    initialization-mode: always
    url: jdbc:mysql://localhost:3306/shoe_in_flux
    username: root
    password: 12345678
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
